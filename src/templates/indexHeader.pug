nav.navbar(role='navigation', aria-label='main navigation')
  .navbar-brand
    a.navbar-item(href='/')
      img(src='/images/WebpointLogoTransparent.png')
      p.home-title &nbsp#{title}
    a.navbar-burger.burger(role='button', aria-label='menu', aria-expanded='false', data-target='navbar')
      span(aria-hidden='true')
      span(aria-hidden='true')  
      span(aria-hidden='true')
  #navbar.navbar-menu
    .navbar-end
      each link in links
        - let [name, url] = link;
        .navbar-item
          a.navbar-item(class={'is-active': page === name}, href=url) #{name}